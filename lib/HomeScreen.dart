import 'package:flutter/material.dart';

class HomeScreen Future<entends> StatelessWidget;  async {
@override
Widget build(BuildContext context){
    return Scaffold(
        appBar: AppBar(title: text("Home Screen),
        ),
        body: Center(
            child: TextButton(
            
            
             
             onPressed: () => logOut(context),
              child: Text("LogOut))),
    ),
    ),
);
}
}

class entends {
}